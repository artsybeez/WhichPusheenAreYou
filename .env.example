# Copy this file to `.env` and fill in your own Stripe TEST secret key.
# Find it at: https://dashboard.stripe.com/test/apikeys
# The .env file is gitignored so your real key never gets committed.
STRIPE_SECRET_KEY=sk_test_your_key_here
